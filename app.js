(function () {
  const STORAGE_KEY = 'draw-card-pools';
  const SAVE_OPTION_KEY = 'draw-card-save-option';
  const THEME_KEY = 'draw-card-theme';

  const cardPools = {
    character: [],
    relationship: [],
    personality: [],
    story: []
  };

  const REPLENISH_MAX = 6;

  const limits = {
    character: { draw: 3, keep: 2, discard: 1 },
    relationship: { draw: 2, keep: 1, discard: 1 },
    personality: { draw: 3, keep: 2, discard: 1 },
    story: { draw: 4, keep: 4, discard: 0 }
  };

  const totalDrawnCount = { character: 0, relationship: 0, personality: 0, story: 0 };

  let currentType = 'character';
  let drawn = { character: [], relationship: [], personality: [], story: [] };
  let kept = { character: [], relationship: [], personality: [], story: [] };
  let currentDeck = {}; // 當前類型未抽完的牌堆（複本）
  let pendingReveal = null;  // 待翻開的那一張
  let discardMode = false;  // 是否在「選一張去掉」模式

  const typeNames = {
    character: '人物卡',
    relationship: '關係卡',
    personality: '個性卡',
    story: '劇情卡'
  };

  const defaultPools = {
    character: [
      '美術系大學生', '音樂系鋼琴主修學生', '高中一年級學生', '高中二年級學生', '高中三年級學生',
      '資訊工程系大學生', '建築系學生', '體育老師', '國文老師', '英文補習班老師', '數學老師', '幼稚園老師',
      '護理師', '急診醫師', '外科醫生', '心理諮商師', '牙醫', '藥師', '生物研究員', '化學工程師', '半導體工程師',
      '軟體工程師', 'UI設計師', '平面設計師', '遊戲企劃', '遊戲實況主', 'YouTuber', '偶像練習生', '職業歌手',
      '樂團吉他手', '作曲家', 'DJ', '舞者', '芭蕾舞演員', '模特兒', '服裝設計師', '彩妝師', '髮型設計師',
      '美甲師', '皮膚管理師', '廚師', '甜點師', '咖啡師', '調酒師', '餐廳店長', '便利商店店員', '百貨專櫃銷售員',
      '房仲業務', '保險業務員', '銀行行員', '會計師', '財務分析師', '股票交易員', '創業老闆', '行銷企劃', '社群小編',
      '公關人員', '記者', '編輯', '作家', '漫畫家', '插畫師', '攝影師', '導演', '演員', '配音員',
      '律師', '檢察官', '法官', '警察', '消防員', '軍人', '空服員', '飛行員', '機師維修工程師', '計程車司機',
      '公車司機', '建築工人', '水電技師', '木工師傅', '農夫', '漁夫', '寵物美容師', '獸醫', '動物訓練師',
      '社工', '心理輔導老師', 'NGO工作人員', '環保研究員', '政治幕僚', '議員助理', '公務員', '圖書館員', '博物館策展人',
      '考古學家', '天文學家', '氣象預報員', '旅遊領隊', '翻譯員', '職業電競選手',
      '國小一年級學生', '國小二年級學生', '國小三年級學生', '國小四年級學生', '國小五年級學生', '國小六年級學生',
      '國中一年級學生', '國中二年級學生', '國中三年級學生', '幼兒園學生', '研究所碩士生', '研究所博士生',
      '法律系大學生', '醫學系大學生', '護理系大學生', '心理系大學生', '教育系大學生', '機械工程系大學生', '電機工程系大學生',
      '土木工程系大學生', '財金系大學生', '企業管理系大學生', '行銷系大學生', '傳播系大學生', '新聞系大學生', '外文系大學生',
      '日文系大學生', '韓文系大學生', '歷史系大學生', '哲學系大學生', '社會系大學生', '政治系大學生', '地理系大學生',
      '物理系大學生', '化學系大學生', '生物系大學生', '數學系大學生', '統計系大學生',
      '護理長', '牙醫助理', '藥局店員', '醫院行政人員', '實驗室技術員', '工廠作業員', '品管工程師', '產品經理', '專案經理',
      '人資專員', '行政助理', '秘書', '客服人員', '電話客服專員', '門市店員', '精品銷售員', '珠寶設計師', '手作藝術家',
      '陶藝家', '雕塑家', '刺青師', '刺繡設計師', '花藝師', '婚禮企劃師', '婚紗攝影師', '活動主持人', '電台主持人',
      '新聞主播', '氣象主播', '電視節目製作人', '影片剪輯師', '動畫師', '3D建模師', '特效師', '遊戲測試員',
      '資安工程師', '資料分析師', '人工智慧工程師', '雲端工程師', '系統管理員', '網路工程師', '電子工程師', '機械工程師',
      '航太工程師', '汽車維修技師', '機車維修技師', '電梯維修技師', '冷氣維修技師', '清潔人員', '保全人員', '大樓管理員',
      '停車場管理員', '快遞員', '外送員', '倉儲管理員', '物流司機', '貨車司機', '船員', '漁船船員', '港口裝卸工',
      '礦工', '石油工程師', '園藝師', '景觀設計師', '室內設計師', '房屋設計師', '房屋建築師', '都市規劃師', '測量員',
      '地政士', '不動產估價師', '證券營業員', '期貨交易員', '基金經理人', '投資顧問', '保母', '月嫂', '看護',
      '養老院照護員', '復健師', '職能治療師', '語言治療師', '營養師', '健身教練', '瑜伽老師', '游泳教練', '籃球教練',
      '足球教練', '棒球教練', '武術教練', '跆拳道教練', '滑雪教練', '潛水教練', '登山嚮導', '旅遊部落客', '美食部落客',
      '時尚部落客', '科技評論員', '影評人', '書評人', '占星師', '塔羅牌師', '命理師', '宗教工作者', '牧師', '和尚', '道士',
      '警校學生', '軍校學生', '飛行學員', '航海系學生', '餐飲系學生', '美容美髮科學生', '設計科學生', '資訊科學生', '電子科學生',
      '汽修科學生', '幼保科學生', '護理科學生', '表演藝術科學生', '體育班學生', '音樂班學生', '美術班學生', '舞蹈班學生'
    ],
    relationship: [
      '師徒', '戀人', '對手', '摯友', '主僕', '同盟', '仇敵', '陌生人', '天降竹馬', '青梅竹馬', '前世情人', '契約戀人',
      '商業聯姻對象', '競爭搭檔', '宿敵', '死對頭', '合作夥伴', '戰友', '革命夥伴', '互相利用關係', '債主與欠債人',
      '救命恩人與被救者', '監護人與被監護人', '指導者與追隨者', '偶像與粉絲', '經紀人與藝人', '投資人與創業者', '上司與下屬',
      '前任戀人', '暗戀對象', '單戀關係', '網友', '室友', '鄰居', '同事', '同班同學', '學長與學弟', '學姊與學妹',
      '前輩與後輩', '競技對手', '訓練夥伴', '拍檔', '搭檔偵探', '警察與線人', '警察與嫌疑人', '律師與委託人', '醫生與病人',
      '心理諮商師與個案', '老師與學生', '家教老師與學生', '導師與研究生', '老闆與員工', '顧客與店員', '租客與房東', '導遊與旅客',
      '司機與乘客', '保鏢與雇主', '刺客與目標', '獵人與獵物', '審問者與被審問者', '審判者與被審判者', '王與騎士', '君主與臣子',
      '騎士與公主', '騎士與魔法師', '魔王與勇者', '英雄與反派', '宿命對手', '命運共同體', '靈魂伴侶', '契約夥伴', '召喚者與召喚物',
      '主人與使魔', '神與信徒', '惡魔與契約者', '守護者與被守護者', '觀察者與實驗體', '研究者與研究對象', '科學家與助手',
      '船長與船員', '隊長與隊員', '教練與選手', '裁判與選手', '製作人與藝人', '導演與演員', '編輯與作者', '畫師與模特',
      '攝影師與被攝者', '記者與受訪者', '主持人與來賓', '評論者與創作者', '粉絲與黑粉', '追求者與被追求者', '告白者與拒絕者',
      '婚約者', '未婚夫妻', '前未婚夫妻', '政治盟友', '政治對手', '談判代表', '外交官與翻譯', '間諜與情報員', '間諜與目標',
      '叛徒與追捕者', '監視者與被監視者', '看守者與囚犯', '典獄長與囚犯', '獄友', '審判官與證人', '證人與辯護者', '辯護律師與檢察官',
      '法官與律師', '競標對手', '商業競爭者', '合作創辦人', '共同繼承人', '利益共同體', '利益衝突者', '債務夥伴', '投資競爭者',
      '市場對手', '品牌代言人與品牌方', '設計師與客戶', '建築師與業主', '醫療搭檔', '研究搭檔', '冒險夥伴', '探險隊成員', '旅伴',
      '室友兼同事', '室友兼對手', '網友轉現實朋友', '網友轉戀人', '筆友', '匿名聊天對象', '遊戲隊友', '遊戲敵對玩家', '公會成員',
      '公會敵對成員', '訓練營同期', '軍中同袍', '軍中對手', '教會同伴', '宗教對立者', '信徒與異端', '預言者與命定之人',
      '時間旅行者與被改變命運者', '未來戀人', '平行世界的自己', '複製人與本體', 'AI與使用者', 'AI與創造者', '機器人與主人',
      '機器人與維修師', '學生會長與成員', '社團社長與社員', '社團競爭者', '樂團成員', '樂團對立樂團', '製作團隊成員', '劇組同事',
      '演出搭檔', '演出競爭者', '舞台對手', '比賽評審與選手', '選秀選手競爭者', '導師與學員', '粉絲與偶像黑粉', '應援夥伴',
      '追星同好', '站姐與藝人', '黑粉與藝人', '守門人與挑戰者', '測試者與參與者', '委託人與執行者', '中間人與雙方當事人',
      '情報販子與買家', '收藏家與贗品商', '藝術家與贊助人', '贊助人與受贊助者', '慈善家與受助者', '救援者與倖存者', '醫護與傷患',
      '志工與服務對象', '導盲者與被引導者', '訓獸師與野獸', '馴龍師與龍', '騎士與坐騎', '召喚師與精靈', '魔法老師與學徒',
      '咒術師與詛咒對象', '巫師與契約精靈', '守墓人與亡靈', '通靈者與靈魂', '驅魔師與惡靈', '吸血鬼與獵人', '狼人與獵人',
      '妖怪與人類朋友', '妖怪與陰陽師', '陰陽師與式神', '神官與神明', '祭司與信徒', '預言家與王', '王位競爭者', '繼承權競爭者',
      '王國同盟者', '敵國將領', '外交使節', '俘虜與看守', '逃亡者與追捕者', '賞金獵人與目標', '委託刺殺者與殺手', '殺手與搭檔',
      '殺手與背叛者', '地下組織成員', '黑幫同盟', '黑幫對立勢力', '情報組織成員', '臥底與目標組織成員', '警察與臥底',
      '記憶喪失者與照顧者', '病患與陪護者', '心理創傷互助者', '復仇者與仇人', '救贖者與被救贖者', '守望者與被守望者',
      '觀眾與表演者', '聽眾與演奏者', '讀者與作者', '玩家與NPC', '玩家與遊戲管理員', '競速對手', '拳擊對手', '棋局對手',
      '舞蹈搭檔', '舞蹈競爭者', '歌唱搭檔', '歌唱對手', '創作搭檔', '創作競爭者', '設計競爭者', '研究競爭者', '同門師兄弟',
      '同門競爭者', '武林盟友', '武林宿敵', '江湖同伴', '江湖仇敵', '冒險公會同伴', '冒險公會競爭者', '任務搭檔', '任務競爭者',
      '護送者與被護送者', '交易雙方', '談判對手', '心理博弈對手', '策略盟友', '短暫同盟', '利益交換關係', '秘密共犯', '犯罪搭檔',
      '審訊官與嫌犯', '嫌犯與證人', '證人保護者與證人', '情報交換者', '秘密守護者與秘密持有者', '命運牽連者', '詛咒連結者',
      '生命共享者', '契約婚姻對象', '假戀人', '假結婚對象', '替身與本尊', '影武者與主人', '保姆與僱主', '管家與主人', '助手與專家',
      '學術合作夥伴', '企業合作夥伴', '品牌合作對象', '節目搭檔', '主持搭檔', '競選搭檔', '政治競爭者', '辯論對手', '論文合作作者',
      '論文審稿者與作者', '導覽員與參觀者', '領航員與隊伍', '導航者與迷路者', '救難隊員與受困者', '探員與嫌疑人', '情報官與特工',
      '特工搭檔'
    ],
    personality: [
      '開朗', '冷靜', '傲嬌', '溫柔', '腹黑', '熱血', '神秘', '天然', '內向', '外向', '活潑', '沉穩', '理性', '感性',
      '樂觀', '悲觀', '害羞', '自信', '自卑', '強勢', '軟弱', '固執', '隨和', '認真', '懶散', '細心', '粗心', '體貼', '冷淡',
      '善良', '毒舌', '幽默', '嚴肅', '成熟', '幼稚', '單純', '世故', '浪漫', '現實', '衝動', '謹慎', '勇敢', '膽小', '獨立', '依賴',
      '忠誠', '叛逆', '坦率', '虛偽', '直率', '委婉', '好勝', '佛系', '完美主義', '隨便', '潔癖', '邋遢', '愛撒嬌', '愛逞強',
      '多疑', '信任他人', '敏感', '遲鈍', '愛哭', '堅強', '忍耐', '急躁', '耐心', '溫和', '暴躁', '親切', '疏離', '社交型', '孤僻',
      '領導型', '跟隨型', '保守', '冒險', '理智', '夢幻', '專注', '分心', '自律', '放縱', '慷慨', '小氣', '大方', '計較',
      '寬容', '記仇', '善解人意', '不解風情', '觀察力強', '遲鈍呆萌', '愛幻想', '務實', '有野心', '無欲無求', '競爭心強', '和平主義',
      '責任感強', '逃避型', '主動', '被動', '愛表現', '低調', '正直', '狡猾', '誠實', '說謊成性', '重感情', '薄情', '專一', '花心',
      '慢熱', '熱情', '冷酷', '溫暖', '霸道', '溫順', '守規矩', '叛逆不羈', '樂於助人', '自私', '理想主義', '犬系', '貓系',
      '天然呆', '吐槽役', '氣氛製造者', '氣氛破壞者', '天然黑', '病嬌', '傲慢', '謙虛', '優雅', '粗魯', '紳士', '孩子氣', '母性', '父性',
      '知性', '呆萌', '搞笑', '嚴格', '寵溺', '強迫症', '隨性', '孤傲', '親和力強', '高冷', '慢條斯理', '雷厲風行', '多愁善感', '沒心沒肺',
      '樂天派', '陰沉', '沉默寡言', '話多', '八卦', '守口如瓶', '有原則', '牆頭草', '忠厚老實', '精明', '精打細算', '慵懶', '勤奮',
      '拚命三郎', '享樂主義', '節儉', '揮霍', '膽大', '膽怯', '自戀', '自我厭惡', '善於表達', '不善言辭', '理財能力強', '花錢無度',
      '包容', '控制欲強', '佔有慾強', '無拘無束', '守護型', '依附型', '攻擊性強', '溫吞', '機靈', '笨拙', '記性好', '健忘',
      '容易緊張', '淡定', '臨危不亂', '容易慌張', '行動派', '思考派', '拖延症', '效率高', '理工腦', '藝術氣質', '詩意', '冷幽默',
      '黑色幽默', '自嘲', '愛面子', '厚臉皮', '重承諾', '善變', '專注細節', '大而化之', '規劃型', '隨機應變', '守時', '遲到慣犯',
      '冒失', '穩重', '感恩', '理所當然', '浪子型', '守序型', '混亂型', '天真爛漫', '老成持重', '寡言', '健談', '愛挑戰', '怕麻煩',
      '容易滿足', '永不滿足', '執著', '三分鐘熱度', '記仇型', '健忘型', '講義氣', '現實計算', '忠犬系', '女王氣場', '王者氣場',
      '小惡魔', '老好人', '領袖魅力', '透明人', '觀察者型', '吐槽王', '戲精', '中二病', '治癒系', '壓迫感強', '無害感', '神經質',
      '隨遇而安', '熱心', '冷眼旁觀', '慢性子', '急性子', '好奇心旺盛', '漠不關心', '強硬', '柔軟', '不服輸', '認輸快',
      '抗壓性強', '抗壓性弱', '愛冒險', '求穩定', '保護欲強', '被保護感', '獨佔欲強', '分享型', '內耗型', '自我調節強',
      '情緒化', '情緒穩定', '心思細膩', '大剌剌', '神經大條', '玻璃心', '鐵石心腸', '記恩', '忘恩', '樂於合作', '獨來獨往',
      '正能量', '負能量', '愛計畫', '隨機生活', '容易嫉妒', '不嫉妒', '愛比較', '無比較心', '控制情緒強', '情緒爆發型',
      '自我中心', '為他人著想', '重榮譽', '無所謂榮譽', '守秘密', '愛洩密', '自我要求高', '得過且過', '重效率', '重過程',
      '偏執', '豁達', '專情', '多情'
    ],
    story: [
      '「你只能留在這裡了。」', '「你選他還是選我。」', '「我們是不是在哪裡見過？」', '偶然聽到他和別人打電話的內容', '深夜突然的告白', '被誤會成戀人', '意外重逢', '雨中爭吵', '天台談心', '被迫合作', '任務失敗後的責備', '生死關頭的選擇', '不小心說出的真心話', '失憶後再次相遇', '假裝不認識', '偷偷跟蹤', '深夜求救電話', '錯發訊息', '被抓包說謊', '危機時刻的擁抱', '重傷昏迷', '以為對方死了', '再次出現的舊情人', '突然的求婚', '被迫訂婚', '逃婚', '追逐戲碼', '在車站離別', '機場告別', '最後一通電話', '未寄出的信', '秘密被發現', '「我早就知道了。」', '被背叛的瞬間', '朋友變敵人', '敵人變盟友', '假裝戀人', '真心動搖', '酒後吐真言', '意外親吻', '誤會加深', '爭吵後冷戰', '和解擁抱', '被迫分開', '再次重逢', '共同逃亡', '一起旅行', '迷路', '困在同一個房間', '停電夜晚', '被鎖在電梯', '共度危險任務', '互相救贖', '受傷照顧', '夜晚守護', '「不要離開我。」', '看到對方脆弱的一面', '第一次坦白過去', '家庭衝突', '被家人反對', '私奔計畫', '計畫失敗', '犧牲自己', '替對方頂罪', '被逮捕', '法庭對峙', '真相揭露', '身份曝光', '隱藏身份被發現', '雙重身份衝突', '假死', '目睹死亡', '重生', '時間倒流', '改變命運', '平行世界相遇', '命運預言', '詛咒開始', '解開詛咒', '宿命對決', '最終決戰', '世界崩壞', '拯救世界', '犧牲結局', '幸福結局', '開放結局', '再次相遇的暗示', '記憶恢復', '忘記一切', '重新愛上', '假裝敵對', '暗中保護', '偷偷幫助', '監視任務', '反轉立場', '情感崩潰', '心理對峙', '威脅談判', '利益交換', '「只要你答應我。」', '「這是最後一次。」', '「我不後悔。」', '「你騙我。」', '「原來是你。」', '「我一直在等你。」', '「別走。」', '「對不起。」', '「謝謝你。」', '「我恨你。」', '「我愛你。」',
      '誤會解除', '錯過告白', '延遲的告白', '被迫同居', '室友衝突', '生活磨合', '日常甜蜜', '小爭執', '吃醋', '三角關係', '四角關係', '情敵出現', '情敵合作', '競爭比賽', '一起訓練', '成長蛻變', '挫折低谷', '鼓勵支持', '夢想破滅', '重新振作', '放棄夢想', '重拾夢想', '舊傷復發', '醫院陪伴', '絕症告知', '隱瞞病情', '最後願望', '遺物發現', '過去真相', '童年回憶', '童年約定', '約定破裂', '再次約定', '秘密基地', '共同目標', '任務衝突', '理念對立', '信念崩塌', '自我懷疑', '找回自我', '黑化', '被拯救', '自我犧牲', '反派覺醒', '英雄墮落', '立場交換', '身份互換', '靈魂交換', '偽裝潛入', '被識破', '雙方試探', '心理戰', '追捕行動', '逃亡生活', '躲藏時光', '短暫平靜', '危機再臨', '最後期限', '倒數計時', '犯錯後悔', '補救行動', '贖罪旅程', '原諒', '無法原諒', '分道揚鑣', '再度合作', '無言陪伴', '深夜談話', '星空告白', '海邊約定', '雨中告白', '車禍意外', '突發事件', '英雄救美', '反救援', '角色反轉', '強制分離', '命運重逢', '絕望時刻', '希望出現', '關鍵證據', '破案瞬間', '陷阱設計', '陷阱中招', '反擊成功', '絕地反擊', '犧牲隊友', '被遺棄感', '找到歸屬', '建立信任', '信任破裂', '再度信任', '誓言', '背誓', '永別', '重逢擁抱', '無法觸碰', '禁忌之戀', '隱藏關係', '公開關係', '社會壓力', '世俗阻礙', '命運捉弄', '巧合相遇', '意外發現', '真相拼圖', '記憶碎片', '過去重現', '夢境預示', '噩夢成真', '幻覺迷惑', '心魔對抗', '自我對話', '心理崩潰', '情緒爆發', '冷靜決策', '極端選擇', '犧牲少數', '拯救多數', '道德困境', '良心譴責', '重新做人', '墮落深淵', '被拉回來', '最終審判', '命運終局', '新的開始', '留下伏筆', '隱藏彩蛋', '未來暗示', '第二次機會', '過去改變', '未來崩壞', '世界線修正', '命運收束', '奇蹟發生', '奇蹟代價', '代價揭露', '秘密交易', '禁忌力量', '力量失控', '控制力量', '最強覺醒', '力量消失', '成為普通人', '成為英雄', '成為反派', '成為傳說', '名聲崩壞', '名譽恢復', '輿論壓力', '群眾誤解', '真相公開', '全員震驚', '意料之外', '反轉真相', '最終答案', '心願實現', '心願破滅', '願望代價', '魔法契約', '契約破裂', '靈魂連結', '靈魂分離', '生死相依', '生離死別', '再見約定', '再也不見', '再度開始', '重寫命運', '逆轉結局', '命運輪迴', '永恆循環', '終於結束', '最後一戰', '最後擁抱', '最後微笑', '最後謊言', '最後真心', '遲來的告白', '遲來的道歉', '遲來的理解', '終於理解', '互相理解', '誤會終結', '新危機誕生', '真正敵人出現', '隱藏Boss', '幕後黑手', '真正英雄', '身份揭曉', '過去曝光', '家族秘密', '血統秘密', '禁忌出生', '被詛咒命運', '打破命運', '自由選擇', '重新定義關係', '關係崩壞', '關係重建', '心牆崩塌', '情感爆發', '無聲理解', '眼神交流', '觸碰瞬間', '擁抱治癒', '心跳加速', '情感確認', '不敢承認', '終於承認', '被迫承認', '全世界知道', '秘密守住', '秘密洩漏', '震撼發現', '命運交錯', '交錯人生', '互換人生', '另一種可能', '如果當時', '後悔選擇', '堅定選擇', '無法回頭', '重新開始', '從零開始', '崩潰重建', '破碎重組', '找到答案', '迷失方向', '重新導航', '終點抵達', '新旅程開始', '無法逃避', '必須面對', '終於面對', '勇敢前進', '退縮猶豫', '最後鼓起勇氣', '關鍵時刻出現', '千鈞一髮', '絕處逢生', '命懸一線', '奇蹟生還', '真正勝利', '慘痛代價', '無法挽回', '最後悔恨', '終於釋懷', '心結解開', '互相救贖', '自我救贖', '被他人救贖', '無法被救', '黑暗吞噬', '光明出現', '最終選擇', '命運判決', '自我犧牲結局', '幸福團圓結局', '開放式結局', '苦澀結局', '希望結局', '循環再開', '故事未完', '新篇章開始', '世界改變', '角色改變', '關係升華', '關係破裂', '永遠承諾', '永遠離別', '再次相見', '偶然擦肩', '命運牽引', '奇妙連結', '靈魂共鳴', '無法切斷', '命運鎖鏈', '自由解放', '突破限制', '打破規則', '重新定義世界', '創造未來', '選擇命運', '命運終章', '故事落幕', '餘韻未散', '回憶永存', '另一段故事開始',
      '隱藏真兇', '誤導線索', '調查深入', '真兇自白', '心裡操控', '被操控發現', '反操控', '記憶造假', '記憶植入', '記憶刪除', '找回記憶', '夢與現實混淆', '幻境試煉', '心靈試煉', '信任測試', '忠誠考驗', '極限訓練', '生存遊戲', '密室逃脫', '解謎關卡', '關鍵提示', '錯誤判斷', '逆轉局勢', '最後拼圖', '真相大白', '真相殘酷', '無法接受', '被迫接受', '逐漸理解', '最終理解', '內心和解', '與過去和解', '與自己和解', '放下執念', '堅持執念', '執念毀滅', '執念救贖', '情感爆裂', '理智崩塌', '信念重建', '最終信念', '勝利代價', '勝利喜悅', '勝利空虛', '敗北成長', '敗北絕望', '再戰決心', '重新挑戰', '最終對峙', '命運交會點', '關鍵相遇', '錯過相遇', '再次相遇', '命運改寫', '歷史改變', '世界重啟', '新世界誕生', '舊世界終結', '未來分歧', '時間悖論', '因果循環', '蝴蝶效應', '關鍵決定', '不可逆轉', '最終代價', '無法挽救', '奇蹟逆轉', '希望之光', '黑暗降臨', '最後希望', '最後守護', '最後信任', '最後選擇', '最後命運', '命運終結', '故事延續', '新冒險開始', '下一個旅程', '永遠的約定', '無聲告別', '微笑離開', '眼淚落下', '手緊握', '手放開', '心跳停止', '心跳重啟', '命運連結', '命運解放', '最終答案揭曉', '所有人改變', '世界見證', '傳說開始', '傳說結束', '英雄誕生', '英雄隕落', '反派救贖', '反派勝利', '真正和平', '暫時和平', '未來仍未知'
    ]
  };

  function loadSavedPools() {
    try {
      const raw = localStorage.getItem(STORAGE_KEY);
      if (raw) {
        const data = JSON.parse(raw);
        ['character', 'relationship', 'personality', 'story'].forEach(k => {
          if (Array.isArray(data[k]) && data[k].length > 0) cardPools[k] = data[k].filter(Boolean);
          else cardPools[k] = [...(defaultPools[k] || [])];
        });
      } else {
        Object.keys(defaultPools).forEach(k => { cardPools[k] = [...defaultPools[k]]; });
      }
    } catch (_) {
      Object.keys(defaultPools).forEach(k => { cardPools[k] = [...defaultPools[k]]; });
    }
  }

  function savePools() {
    if (!document.getElementById('save-settings').checked) return;
    localStorage.setItem(STORAGE_KEY, JSON.stringify(cardPools));
    localStorage.setItem(SAVE_OPTION_KEY, '1');
  }

  function loadSaveOption() {
    const checked = localStorage.getItem(SAVE_OPTION_KEY) === '1';
    const el = document.getElementById('save-settings');
    if (el) el.checked = checked;
  }

  function parsePoolText(text) {
    return text.split(/\n/).map(s => s.trim()).filter(Boolean);
  }

  function getPool(type) {
    const list = cardPools[type] || [];
    return list.length ? [...list] : [typeNames[type] + '（未設定）'];
  }

  function shuffle(arr) {
    const a = [...arr];
    for (let i = a.length - 1; i > 0; i--) {
      const j = Math.floor(Math.random() * (i + 1));
      [a[i], a[j]] = [a[j], a[i]];
    }
    return a;
  }

  function initDeckForType(type) {
    const pool = getPool(type);
    currentDeck[type] = shuffle(pool);
  }

  function ensureDeck(type) {
    if (!currentDeck[type] || currentDeck[type].length === 0) {
      initDeckForType(type);
    }
  }

  function drawOne(type) {
    ensureDeck(type);
    const deck = currentDeck[type];
    if (deck.length === 0) return null;
    const card = deck.pop();
    totalDrawnCount[type]++;
    drawn[type].push({ text: card, revealed: false });
    return card;
  }

  function revealAllPending(type) {
    drawn[type].forEach(c => { c.revealed = true; });
    pendingReveal = null;
    if (type === 'story' && drawn[type].length === 4) {
      drawn[type].forEach(c => kept[type].push(c.text));
      drawn[type].length = 0;
    }
    renderDrawnCards(type);
    updateDrawUI();
  }

  function keepCard(type, index) {
    const item = drawn[type][index];
    if (!item) return;
    kept[type].push(item.text);
    drawn[type].splice(index, 1);
    discardMode = false;
    renderDrawnCards(type);
    updateDrawUI();
  }

  function replenishOne(type) {
    const limit = limits[type];
    if (limit.discard === 0 || totalDrawnCount[type] >= REPLENISH_MAX) return;
    const card = drawOne(type);
    if (!card) return;
    drawn[type][drawn[type].length - 1].revealed = true;
    kept[type].forEach(t => drawn[type].unshift({ text: t, revealed: true }));
    kept[type].length = 0;
    discardMode = true;
    renderDrawnCards(type);
    updateDrawUI();
  }

  function discardByIndex(type, index) {
    const limit = limits[type];
    const removed = drawn[type].splice(index, 1)[0];
    if (kept[type].length >= limit.keep) {
      kept[type].length = 0;
    }
    drawn[type].forEach(c => {
      kept[type].push(c.text);
    });
    drawn[type].length = 0;
    discardMode = false;
    renderDrawnCards(type);
    updateDrawUI();
  }

  function clearAll() {
    drawn = { character: [], relationship: [], personality: [], story: [] };
    kept = { character: [], relationship: [], personality: [], story: [] };
    currentDeck = {};
    totalDrawnCount.character = 0;
    totalDrawnCount.relationship = 0;
    totalDrawnCount.personality = 0;
    totalDrawnCount.story = 0;
    pendingReveal = null;
    discardMode = false;
    ['character', 'relationship', 'personality', 'story'].forEach(type => {
      renderDrawnCards(type);
    });
    updateDrawUI();
  }

  function isAllComplete() {
    return kept.character.length === 2 && kept.relationship.length === 1
      && kept.personality.length === 2 && kept.story.length === 4;
  }

  function updateDrawUI() {
    const btnSpread = document.getElementById('btn-spread');
    if (btnSpread) btnSpread.classList.toggle('hidden', !isAllComplete());

    const type = currentType;
    const limit = limits[type];
    const drawnCount = drawn[type].length;
    const keptCount = kept[type].length;
    const allRevealed = drawn[type].every(c => c.revealed);
    const isReplenishDiscard = limit.discard > 0 && drawnCount === limit.keep + 1 && keptCount === 0 && allRevealed;
    const needDiscard = (limit.discard > 0 && drawnCount === limit.draw && allRevealed) || isReplenishDiscard;
    const storyDone = type === 'story' && keptCount === 4;

    const btnDraw = document.querySelector('.btn-draw');
    const btnReveal = document.querySelector('.btn-reveal');
    const btnDiscard = document.querySelector('.btn-discard');
    const deckPrompt = document.querySelector('.deck-prompt');

    const btnReplenish = document.getElementById('btn-replenish');
    
    // 檢查是否已達到補牌上限
    const reachedMax = totalDrawnCount[type] >= REPLENISH_MAX;
    const canReplenish = limit.discard > 0 && keptCount === limit.keep && drawnCount === 0
      && totalDrawnCount[type] < REPLENISH_MAX;

    if (btnReplenish) btnReplenish.classList.toggle('hidden', !canReplenish);

    // 1. 劇情卡完成判斷
    if (storyDone) {
      btnDraw.disabled = true;
      if (btnReplenish) btnReplenish.classList.add('hidden');
      btnReveal.classList.add('hidden');
      btnDiscard.classList.add('hidden');
      deckPrompt.textContent = '劇情卡已抽完 4 張';
      return;
    }

    // 2. 新增：判斷是否達到補牌次數極限且已完成當前挑選
    if (reachedMax && drawnCount === 0 && keptCount === limit.keep) {
      btnDraw.disabled = true;
      if (btnReplenish) btnReplenish.classList.add('hidden');
      btnReveal.classList.add('hidden');
      btnDiscard.classList.add('hidden');
      deckPrompt.textContent = `已達補牌上限 (${REPLENISH_MAX}張)，無法繼續抽取。若不滿意請「全部清除重抽」。`;
      return;
    }

    // 3. 原有的抽牌與丟棄邏輯
    if (drawnCount >= limit.draw || isReplenishDiscard) {
      btnDraw.disabled = true;
      if (btnReplenish) btnReplenish.classList.add('hidden');
      if (!allRevealed) {
        btnReveal.classList.remove('hidden');
        deckPrompt.textContent = '已抽滿，請點「翻開揭曉」';
      } else if (needDiscard) {
        discardMode = true;
        renderDrawnCards(type);
        btnReveal.classList.add('hidden');
        btnDiscard.classList.remove('hidden');
        btnDiscard.disabled = !drawn[type].some(c => c.discardSelected);
        deckPrompt.textContent = '請點選要去掉的那一張牌，再按「去掉一張」';
      } else {
        btnReveal.classList.add('hidden');
        btnDiscard.classList.add('hidden');
        if (type === 'story') deckPrompt.textContent = '劇情卡已抽完 4 張';
        else deckPrompt.textContent = '此類型已完成';
      }
    } else {
      btnDraw.disabled = canReplenish;
      btnReveal.classList.add('hidden');
      btnDiscard.classList.add('hidden');
      if (canReplenish) {
        deckPrompt.textContent = `可補牌（本組已抽 ${totalDrawnCount[type]} / ${REPLENISH_MAX} 張），抽一張後揭曉並丟棄一張`;
      } else {
        // 如果還沒抽滿且已達上限 (這種情況通常發生在抽牌中途)
        if (reachedMax) {
          deckPrompt.textContent = `已達次數上限，請完成目前的挑選。`;
        } else {
          deckPrompt.textContent = keptCount >= limit.keep
            ? `補牌：再抽 ${limit.draw - drawnCount} 張，可重新選留 ${limit.keep} 張`
            : `已抽 ${drawnCount} / ${limit.draw} 張，點「抽一張」繼續`;
        }
      }
    }
  }

  function renderDrawnCards(type) {
    const blocks = document.querySelectorAll('.drawn-cards[data-type="' + type + '"]');
    const block = blocks[0];
    if (!block) return;
    const drawnList = block.querySelector('.drawn-list');
    const keptList = block.querySelector('.kept-list');
    if (!drawnList || !keptList) return;

    const limit = limits[type];
    const isReplenishDiscard = limit.discard > 0 && drawn[type].length === limit.keep + 1
      && kept[type].length === 0 && drawn[type].every(c => c.revealed);
    const needDiscard = (limit.discard > 0 && drawn[type].length === limit.draw && drawn[type].every(c => c.revealed))
      || isReplenishDiscard;

    drawnList.innerHTML = drawn[type].map((c, i) => {
      const selectable = needDiscard && discardMode;
      const selected = selectable && c.discardSelected;
      return `<div class="card-item ${c.revealed ? 'revealed' : 'back'} ${selected ? 'discard-candidate' : ''}" data-type="${type}" data-index="${i}" data-selectable="${selectable}">${c.revealed ? `<span class="card-label">${escapeHtml(c.text)}</span>` : '?'}</div>`;
    }).join('');

    keptList.innerHTML = kept[type].map(text => `<div class="card-item revealed"><span class="card-label">${escapeHtml(text)}</span></div>`).join('');

    drawnList.querySelectorAll('.card-item').forEach(el => {
      el.addEventListener('click', function () {
        const t = this.getAttribute('data-type');
        const idx = parseInt(this.getAttribute('data-index'), 10);
        const selectable = this.getAttribute('data-selectable') === 'true';
        if (selectable) {
          drawn[t].forEach((c, i) => { c.discardSelected = i === idx; });
          renderDrawnCards(t);
          document.querySelector('.btn-discard').disabled = false;
        }
      });
    });
  }

  function escapeHtml(s) {
    const div = document.createElement('div');
    div.textContent = s;
    return div.innerHTML;
  }

  function applySettings() {
    cardPools.character = parsePoolText(document.getElementById('pool-character').value);
    cardPools.relationship = parsePoolText(document.getElementById('pool-relationship').value);
    cardPools.personality = parsePoolText(document.getElementById('pool-personality').value);
    cardPools.story = parsePoolText(document.getElementById('pool-story').value);
    savePools();
    currentDeck = {};
    alert('已套用設定，抽卡頁將使用新卡池。');
  }

  function initNavigation() {
    document.querySelectorAll('.nav-btn').forEach(btn => {
      btn.addEventListener('click', function () {
        const page = this.getAttribute('data-page');
        document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
        this.classList.add('active');
        document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
        document.getElementById('page-' + page).classList.add('active');
      });
    });
  }

  function initTypeTabs() {
    document.querySelectorAll('.type-tab').forEach(tab => {
      tab.addEventListener('click', function () {
        const type = this.getAttribute('data-type');
        currentType = type;
        document.querySelectorAll('.type-tab').forEach(t => t.classList.remove('active'));
        this.classList.add('active');
        document.querySelectorAll('.rules-text').forEach(r => {
          r.classList.toggle('hidden', r.getAttribute('data-rules') !== type);
        });
        document.querySelectorAll('.drawn-cards').forEach(block => {
          block.classList.toggle('hidden', block.getAttribute('data-type') !== type);
        });
        discardMode = false;
        updateDrawUI();
      });
    });
  }

  function initDrawButtons() {
    document.querySelector('.btn-draw').addEventListener('click', function () {
      const card = drawOne(currentType);
      if (card !== null) {
        renderDrawnCards(currentType);
        updateDrawUI();
      }
    });

    const btnReplenish = document.getElementById('btn-replenish');
    if (btnReplenish) btnReplenish.addEventListener('click', function () {
      replenishOne(currentType);
    });

    document.querySelector('.btn-reveal').addEventListener('click', function () {
      revealAllPending(currentType);
      updateDrawUI();
    });

    document.querySelector('.btn-discard').addEventListener('click', function () {
      const type = currentType;
      const idx = drawn[type].findIndex(c => c.discardSelected);
      if (idx >= 0) {
        discardByIndex(type, idx);
      }
    });

    document.querySelector('.btn-clear').addEventListener('click', clearAll);
  }

  function renderSpread() {
    document.querySelectorAll('.spread-slot').forEach(slot => {
      const type = slot.getAttribute('data-type');
      const idx = parseInt(slot.getAttribute('data-idx'), 10);
      const text = (kept[type] && kept[type][idx]) ? kept[type][idx] : '';
      slot.textContent = text;
      slot.classList.remove('selected');
    });
  }

  function openSpread() {
    document.getElementById('spread-overlay').classList.remove('hidden');
    renderSpread();
  }

  function initSpread() {
    const btnSpread = document.getElementById('btn-spread');
    const overlay = document.getElementById('spread-overlay');
    const btnClose = document.getElementById('btn-close-spread');

    if (btnSpread) btnSpread.addEventListener('click', openSpread);
    if (btnClose) btnClose.addEventListener('click', function () {
      overlay.classList.add('hidden');
      selectedSpreadSlot = null;
      ['character', 'relationship', 'personality', 'story'].forEach(t => renderDrawnCards(t));
    });
    if (overlay) overlay.addEventListener('click', function (e) {
      if (e.target === overlay) {
        overlay.classList.add('hidden');
        selectedSpreadSlot = null;
        ['character', 'relationship', 'personality', 'story'].forEach(t => renderDrawnCards(t));
      }
    });

    let selectedSpreadSlot = null;
    document.querySelectorAll('.spread-slot').forEach(slot => {
      slot.addEventListener('click', function (e) {
        e.stopPropagation();
        const type = this.getAttribute('data-type');
        const idx = parseInt(this.getAttribute('data-idx'), 10);

        if (selectedSpreadSlot === this) {
          selectedSpreadSlot.classList.remove('selected');
          selectedSpreadSlot = null;
          return;
        }
        if (selectedSpreadSlot) {
          const prevType = selectedSpreadSlot.getAttribute('data-type');
          const prevIdx = parseInt(selectedSpreadSlot.getAttribute('data-idx'), 10);
          if (prevType === type && prevIdx !== idx) {
            const arr = kept[type];
            if (arr && arr[prevIdx] !== undefined && arr[idx] !== undefined) {
              const t = arr[prevIdx];
              arr[prevIdx] = arr[idx];
              arr[idx] = t;
              renderSpread();
            }
          }
          selectedSpreadSlot.classList.remove('selected');
          selectedSpreadSlot = null;
        } else {
          selectedSpreadSlot = this;
          this.classList.add('selected');
        }
      });
    });
  }

  function initSettings() {
    document.querySelector('.btn-apply').addEventListener('click', applySettings);
    const btnRestore = document.getElementById('btn-restore');
    if (btnRestore) btnRestore.addEventListener('click', restoreDefaultPools);
    document.getElementById('save-settings').addEventListener('change', function () {
      localStorage.setItem(SAVE_OPTION_KEY, this.checked ? '1' : '0');
      if (this.checked) savePools();
    });
  }

  function fillSettingsFromPools() {
    document.getElementById('pool-character').value = (cardPools.character || []).join('\n');
    document.getElementById('pool-relationship').value = (cardPools.relationship || []).join('\n');
    document.getElementById('pool-personality').value = (cardPools.personality || []).join('\n');
    document.getElementById('pool-story').value = (cardPools.story || []).join('\n');
  }

  function restoreDefaultPools() {
    document.getElementById('pool-character').value = (defaultPools.character || []).join('\n');
    document.getElementById('pool-relationship').value = (defaultPools.relationship || []).join('\n');
    document.getElementById('pool-personality').value = (defaultPools.personality || []).join('\n');
    document.getElementById('pool-story').value = (defaultPools.story || []).join('\n');
  }

  function applyTheme(light) {
    document.documentElement.setAttribute('data-theme', light ? 'light' : 'dark');
    const btn = document.getElementById('theme-toggle');
    if (btn) btn.textContent = light ? '☀️ 深色' : '🌙 淺色';
    try { localStorage.setItem(THEME_KEY, light ? 'light' : 'dark'); } catch (_) {}
  }

  function initTheme() {
    const saved = localStorage.getItem(THEME_KEY);
    const wantLight = saved !== 'dark';
    applyTheme(wantLight);
    const btn = document.getElementById('theme-toggle');
    if (btn) btn.addEventListener('click', function () {
      const isLight = document.documentElement.getAttribute('data-theme') === 'light';
      applyTheme(!isLight);
    });
  }

  function init() {
    loadSavedPools();
    loadSaveOption();
    initTheme();
    fillSettingsFromPools();
    initNavigation();
    initTypeTabs();
    initDrawButtons();
    initSpread();
    initSettings();
    updateDrawUI();
  }

  init();
})();
